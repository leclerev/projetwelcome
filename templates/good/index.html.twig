{% extends 'base.html.twig' %}

 {% block title %}
     {#Hello {{ controller_name }}#}
     <div class="titre">
         <h1>Welcome</h1>
         <h2>Comme à la maison, mais ailleurs !</h2>
     </div>
 {% endblock %}



{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">


        <table>
            <tr>
                {% for key, good in goods %}
                    {{ (key%3)?'':'</tr><tr>' }}
                    <td>
                        <h2>{{ good.getDescription() }}</h2>
                        {#<img src="{{ asset('documents/image1.png') }}" alt="Smiley face" height="150" width="150">#}
                        <img src="{{ asset(good.photo) }}" height="150" width="150">
                        <div>

                            {{ typeGood[good.getTypePropertie()] }}
                            <br>
                            {{ good.getAddress().getCity() }}
                            <br>
                        </div>
                    </td>

                {% endfor %}
            </tr>
        </table>
    </div>
{% endblock %}
