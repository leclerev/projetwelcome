{% extends 'base.html.twig' %}

{% block title %}
    {#Hello {{ controller_name }}#}
    <div class="titre">
        <h1>Welcome</h1>
        <h2>Comme à la maison, mais ailleurs !</h2>
    </div>
{% endblock %}



{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">


        <table>
            <tr>
                {% for key, good in goods %}
                    {{ (key%3)?'':'</tr><tr>' }}
                    <td>
                        <img src="{{ asset('documents/image1.png') }}" alt="Smiley face" height="150" width="150">
                        <div>
                            {{ good.getTypePropertie() }}

                            {{ good.getAddress().getCity() }}

                            {{ good.getDescription() }}
                        </div>
                    </td>

                {% endfor %}
            </tr>
        </table>
        This friendly message is coming from: To Be Continued...
        <ul>
            <li>Your controller at <code><a
                            href="{{ '/Applications/MAMP/htdocs/Symfony/src/Controller/GoodController.php'|file_link(0) }}">src/Controller/GoodController.php</a></code>
            </li>
            <li>Your template at <code><a
                            href="{{ '/Applications/MAMP/htdocs/Symfony/templates/good/index.html.twig'|file_link(0) }}">templates/good/index.html.twig</a></code>
            </li>
        </ul>
        <a href="/createGood">Create a Good</a>
    </div>
{% endblock %}
